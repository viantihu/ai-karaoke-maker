[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# Keep this file minimal so Streamlit Cloud uses `requirements.txt` for
# dependency installation. Do NOT define a [project] table here, otherwise
# Streamlit may attempt to install the repository as a package.
